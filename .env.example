# GitHub Link Tracker Configuration
# ================================

# GitHub Personal Access Token (required)
# Create one at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=your-github-token

# Data Repository Configuration
# =============================
# These settings point to the GitHub repository where your links/issues and tags/labels are stored.
# By default, this was the same as the code repository, but you can now separate them.
#
# For new installations: Create a dedicated data repository for your bookmarks
# For existing installations: After migration, update these to point to your new data repository
GITHUB_OWNER=githubusername
GITHUB_REPO=github-repo-name

# GitHub API endpoint (usually no need to change)
GITHUB_API=https://api.github.com
